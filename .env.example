# Frontend (Vite) env vars — these are PUBLIC, fine to ship to browser
# After deploying Partykit, you'll get a URL like shithead-party.YOURNAME.partykit.dev
# Just the host, no https:// or trailing slash. Leave blank for local dev (defaults to 127.0.0.1:1999).
VITE_PARTYKIT_HOST=
